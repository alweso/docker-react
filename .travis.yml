language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t 850324:docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true apascaru/docker-react npm run test
